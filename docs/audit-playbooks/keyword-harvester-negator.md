# Keyword harvester and negator playbook

Implementation status: **Scaffolded only.** The `/kw-harvester-negator` source package was not provided. The supplied PPC overview confirms that STR must lead and that harvest/negative math lives in `references/str-process.md`; those detailed rules are missing.

Required source: the original `/kw-harvester-negator` `.skill` or `.zip` package and the PPC operator's `references/str-process.md`.

## Harvest

A harvest review needs STR evidence. No order, sales, bid, or inventory threshold is active until the source package and the referenced deterministic math are available. Campaign structure and current target coverage should be checked with the Targeting Report.

## Negative review

A negative review needs STR evidence of material spend and clicks without orders. The supplied overview defines a dynamic test-spend and min-click framework, not a fixed `$15 + 8 clicks` rule. No negative threshold or action is active until the complete source is available.

SQP and Cerebro may expand investigation, but cannot prove waste or create a hard negative. Every recommendation is editable, manager-approved, evidence-linked, and non-executable until an authorized Amazon Ads integration exists.
